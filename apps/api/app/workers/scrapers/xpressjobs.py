"""XpressJobs is a React SPA — real listings require Playwright. Skipped to avoid duplicate demo data."""

from typing import Any


async def scrape_xpressjobs(limit: int = 40) -> list[dict[str, Any]]:
    return []
